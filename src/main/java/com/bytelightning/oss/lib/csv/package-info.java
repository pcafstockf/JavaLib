/**
 * The code in this package is heavily borrowed from the Apache Commons CSV project.
 * The classes in this package have been modified to allow for efficiently parsing a single (or few) lines at a time.
 * (I initially used them to parse incoming async csv with Netty).
 */
package com.bytelightning.oss.lib.csv;